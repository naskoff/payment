{% extends 'base.html.twig' %}

{% block title %}Create new post{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            {{ form_start(form, {'attr': {'action': path('create_post')}}) }}
            {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
